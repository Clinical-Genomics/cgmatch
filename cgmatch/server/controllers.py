import logging
LOG = logging.getLogger(__name__)


def benchmark_patients(id=None):
    """ Return one or more benchmark patients objects

    Args:
        id(str): patient id

    Returns:
        patient_objs(list): a list of benchmark patient objects

    """
    return "Ecco i benchmark patients!"
